exports.BattleStatuses = {
	// "intrisics"
	gengar: {
		onImmunity: function (type) {
			if (type === 'Ground') return false;
		}
	},
	gengarmega: {
		onImmunity: function (type) {
			if (type === 'Ground') return false;
		}
	},
	mewtwomegay: {
		onImmunity: function (type) {
			if (type === 'Ground') return false;
		}
	},
	klefki: {
		onImmunity: function (type) {
			if (type === 'Ground') return false;
		}
	},
}